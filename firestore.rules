rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Open access to allow seamless unauthenticated visitor guest sessions and model comparison
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
